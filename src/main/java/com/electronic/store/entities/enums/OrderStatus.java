package com.electronic.store.entities.enums;

public enum OrderStatus {
    PLACED,
    DISPATCHED,
    SHIPPED,
    DELIVERED,
    PENDING,
    CANCELLED,
    RETURN_REQUESTED,
    RETURN_APPROVED,
    RETURNED,
    RETURN_REJECTED
}